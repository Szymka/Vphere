<view class="weui-cells__title">签到事项</view>
        <view class="weui-cells weui-cells_after-title" wx:for="{{list}}" wx:key="list">
            <!-- <view class="weui-cell">
                <view class="weui-cell__bd">{{item.group_name}}</view>
                <view class="weui-cell__ft">{{item.start_time}} {{item.end_time}} {{item.location}} </view>
                <view class="weui-cell__ft">{{status==0?'未开始':(status==1?'签到成功':'迟到')}}</view>
            </view> -->
        </view>